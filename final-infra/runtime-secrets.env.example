# Copy to runtime-secrets.env. This file is gitignored.
# POSTGRES_PASSWORD may be omitted; bootstrap then generates a random value.
POSTGRES_PASSWORD=replace-with-a-long-random-password

# Required for the Telegram alert demonstration.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
